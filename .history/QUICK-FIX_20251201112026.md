# 🔧 Quick Fix Guide

## Vấn đề hiện tại:
1. ❌ Frontend lock file (process đang chạy ở port 3000)
2. ❌ Backend dist chưa có code mới với logging

## Giải pháp:

### 1. Stop tất cả process đang chạy

Mở PowerShell và chạy:

```powershell
# Kill process trên port 3000 (frontend)
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess | ForEach-Object { Stop-Process -Id $_ -Force }

# Kill process trên port 5000 (backend)
Get-NetTCPConnection -LocalPort 5000 -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess | ForEach-Object { Stop-Process -Id $_ -Force }
```

### 2. Build và Copy Backend

```powershell
cd "D:\DiskE\DU AN CONTAINER\containerreseu\backend"

# Copy JS files
node copy-js-files.js

# Build TypeScript
npm run build

# Start backend
npm start
```

### 3. Start Frontend (Terminal riêng)

```powershell
cd "D:\DiskE\DU AN CONTAINER\containerreseu\frontend"

# Xóa lock file nếu cần
Remove-Item -Path ".next\dev\lock" -ErrorAction SilentlyContinue

# Start frontend
npm run dev
```

### 4. Mở Debug Page

Sau khi cả 2 services đã chạy, mở browser:

**http://localhost:3000/dashboard/debug-data**

Trang này sẽ cho bạn biết:
- ✅ Depot nào có container
- ✅ Depot ID và Container Depot ID có match không
- ✅ Số lượng container thực tế vs kỳ vọng

### 5. Test Container List

Từ debug page, tìm depot có `actual > 0` containers.

Sau đó vào: **http://localhost:3000/dashboard/containers**

Click vào depot đó và xem danh sách container.

## Kiểm tra Log

### Backend Console:
```
✅ Reuse container list retrieved successfully
📊 Containers count: XXX
📍 Containers by Depot ID: { '15': 50, '1': 30, ... }
```

### Browser Console (F12):
```
[ContainerService] ===== FETCHING CONTAINERS FOR DEPOT =====
[ContainerService] Target depot ID: 15 Type: string
[ContainerService] Total containers from API: XXX
[ContainerService] All unique depot IDs in data: [15, 1, 3, ...]
[ContainerService] ✅ MATCH FOUND: ...
[ContainerService] Filtered containers count: XXX
```

## Nếu vẫn không thấy container:

1. Check debug page - xem có depot nào có containers không
2. Kiểm tra depot ID trong URL có match với depot ID trong data không
3. Check backend log xem API call có thành công không
